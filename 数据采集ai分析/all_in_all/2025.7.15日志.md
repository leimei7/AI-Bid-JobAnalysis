## 2025.7.15

### 修改buy_urls.py爬虫输入参数，实现信息类型枚举，默认中标类型。

### 修改数据库第一表的json_data结构，增加公告类型信息。

### 增加buy_urls.py爬虫输入参数，开始时间，截止时间，默认爬取当前时间。

### 优化ai_data.py爬虫处理数据逻辑，增加采购人信息，采购代理机构信息，项目联系方式等信息。

### 优化read_csv.py人工智能数据分析逻辑，剔除extracted_data的无关量，减少传入token数量，减轻压力提升效率。

### 优化read_csv.py人工智能数据分析逻辑，分解res的数据结构，减少输出token数量，再通过json.dumps合并数据结构，提高程序效率。

### 增加get_urls.py的爬虫数据文件管理，避免数据冲突，或者遗失。

### 修改get_boss_all.py的csv文件保存逻辑，修复了没有职位就不生成对应文件的错误逻辑。

### 优化read_csv.py处理数据最大量问题，防止传入数据量溢出，添加分批处理数据再合并的功能。

### 优化get_boss_all.py爬虫中断判定，以单页最小量为判断条件，精准快速中断无效爬虫，提高程序效率。

### 增加get_boss_all.py爬虫zp_stoken的复用功能，防止有效token的浪费，避免资源浪费，减少爬取次数，降低爬虫可疑度。

### 增加get_boss_all.py功能函数mark_data_as_processed()，将处理过的数据库数据标记，防止反复处理。
